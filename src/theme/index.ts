import { colors } from './colors';
import { webDimensions } from './webDimensions';

const theme = {
  colors,
  webDimensions,
  breakpoints: {
    desktop: '1279px',
    tablet: '880px',
    mobile: '425px',
  },
};

export default theme;
